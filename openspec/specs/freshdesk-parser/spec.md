# Freshdesk Parser Specification

## Purpose

Эта спецификация описывает функциональность парсера Freshdesk для образовательного проекта. Парсер предназначен для извлечения данных тикетов из Freshdesk API и сохранения их в локальной базе данных для дальнейшей обработки и анализа.

Спецификация следует принципам Specification-Driven Development и создана в соответствии с архитектурными принципами проекта, описанными в [openspec/project.md](../../project.md) и [.codeassistant/rules/architecture.md](../../../.codeassistant/rules/architecture.md).
## Requirements
### Requirement: Каркас проекта
Система SHALL предоставлять возможность постраничной загрузки задач из Freshdesk API с размером страницы 100 записей.

#### Scenario: Успешная прохождение тестов
Given требуется выполнить unit-test
When запускается команда проверки тестов
Then все тесты пройдены успешно

### Requirement: Постраничная загрузка данных из Freshdesk API
Система SHALL предоставлять возможность постраничной загрузки задач из Freshdesk API с размером страницы 100 записей.
// Система ДОЛЖНА предоставлять возможность постраничной загрузки задач из Freshdesk API с размером страницы 100 записей.

#### Scenario: Успешная загрузка первой страницы задач
Given система имеет доступ к Freshdesk API
When запрашивается первая страница задач (page=1, per_page=100)
Then система получает массив из до 100 задач в формате JSON

#### Scenario: Загрузка последующих страниц задач
Given система успешно загрузила предыдущую страницу
When запрашивается следующая страница задач
Then система получает следующую порцию задач или пустой массив

### Requirement: Сохранение JSON файлов
Система SHALL сохранять полученные данные от Freshdesk API в файлы формата JSON.
// Система ДОЛЖНА сохранять полученные данные от Freshdesk API в файлы формата JSON.

#### Scenario: Сохранение данных в файл
Given система получила данные от Freshdesk API
When данные сохраняются в файл
Then файл сохраняется в директории backend/storage/freshdesk/ с именем tasks-{номер страницы}.json

#### Scenario: Сохранение оригинальных данных
Given система получила данные от Freshdesk API
When данные сохраняются в файл
Then содержимое файла идентично ответу API без изменений

### Requirement: Задержка между запросами
Система SHALL выдерживать паузу не менее 1 секунды между последовательными запросами к Freshdesk API.
// Система ДОЛЖНА выдерживать паузу не менее 1 секунды между последовательными запросами к Freshdesk API.

#### Scenario: Соблюдение интервала между запросами
Given система отправляет несколько запросов к API
When между запросами проходит пауза
Then пауза составляет не менее 1 секунды

### Requirement: Обработка ошибок API
Система SHALL корректно обрабатывать ошибки авторизации (401) и ошибки превышения лимита запросов (429) от Freshdesk API.
// Система ДОЛЖНА корректно обрабатывать ошибки авторизации (401) и ошибки превышения лимита запросов (429) от Freshdesk API.

#### Scenario: Обработка ошибки авторизации
Given система отправляет запрос с неверными учетными данными
When API возвращает ошибку 401
Then система выбрасывает FreshdeskApiException

#### Scenario: Обработка ошибки лимита запросов
Given система отправляет слишком много запросов
When API возвращает ошибку 429
Then система делает паузу и повторяет запрос

### Requirement: Продолжение парсинга после прерывания
Система SHALL продолжать парсинг задач с последней необработанной страницы при повторном запуске.
// Система ДОЛЖНА продолжать парсинг задач с последней необработанной страницы при повторном запуске.

#### Scenario: Продолжение парсинга со следующей страницы
Given процесс парсинга был прерван на странице N
When запускается новый процесс парсинга
Then процесс начинается со страницы N+1

#### Scenario: Определение последней обработанной страницы
Given в директории backend/storage/freshdesk/ есть файлы tasks-{номер страницы}.json
When запускается процесс парсинга
Then система определяет номер последнего файла и начинает со следующей страницы

### Requirement: Перезапуск парсинга с первой страницы
Система SHALL поддерживать перезапуск парсинга с первой страницы при использовании параметра --force.
// Система ДОЛЖНА поддерживать перезапуск парсинга с первой страницы при использовании параметра --force.

#### Scenario: Перезапуск парсинга с параметром --force
Given существует команда php artisan freshdesk:parse-tasks
When команда запускается с параметром --force
Then все существующие файлы tasks-*.json удаляются и парсинг начинается с первой страницы

#### Scenario: Перезапуск парсинга без параметра --force
Given существует команда php artisan freshdesk:parse-tasks
When команда запускается без параметра --force
Then существующие файлы сохраняются и парсинг продолжается с последней страницы

